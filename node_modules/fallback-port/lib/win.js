
'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _shelljs = require('shelljs');

var _shelljs2 = _interopRequireDefault(_shelljs);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var spawn = require('child_process').spawn;

var win = {
    pid: function pid(port) {
        var result = _shelljs2.default.exec('netstat -anop tcp | findstr ":' + port + '" |  findstr "LISTENING"', { silent: true }).stdout;
        return result ? result.split(/\s+/)[5] : null;
    },

    kill: function kill(port) {
        var pid = win.pid(port);
        pid && spawn("taskkill", ["/pid", pid, '/f', '/t']);
    }
};

exports.default = win;
module.exports = exports['default'];